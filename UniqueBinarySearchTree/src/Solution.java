import java.util.ArrayList;
import java.util.List;

public class Solution {
    public List<TreeNode> buildBST(int n) {
        List<TreeNode> trees = new ArrayList<>();
        for (int i = 1; i <= n; i++) {
            TreeNode root = new TreeNode(i);
            
        }
        return trees;
    }
}
